apiVersion: v1
kind: Pod
metadata:
  name: scatpythonserver
  namespace: scat
  labels:
    purpose: searchService
spec:
  containers:
    - image: pswargam/scatpythonserver
      name: scatpythonserver
      ports:
        - containerPort: 8000
