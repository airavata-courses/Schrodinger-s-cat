version: '2'

services:
  web:
    build: .
    image: reactmicroservice
    command: bash -c "npm install && npm start"
    volumes:
      - .:/code
    ports:
      - "3000:3000"
    network_mode: 'host'