apiVersion: v1
kind: Service
metadata:
  name: servicenodeserver
spec:
  selector:
    purpose: node-server
  type: NodePort
  ports:
    - name: http
      nodePort: 31003
      port: 8012
      targetPort: 4000
      protocol: TCP
